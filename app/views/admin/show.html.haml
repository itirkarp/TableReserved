= content_for :title, "Administration"

= render "shared/header"

#page
  = render "restaurant_form"
  #restaurants
    %h3 Existing Restaurants :
    = render :partial => "restaurant", :collection => @restaurants
