= content_for :title, 'Forgot Password'

#header
  #logo
    = image_tag "reserved.jpg", :id => 'logo-image'
#gap
#page
  #forgot-password
    = form_tag generate_password_url do
      If you have forgotten your password, enter the email address you used to register on Table Reserved and an email with a temporary password will be sent to that address. You can login with this new password.
      %p
      To change the temporary password, visit the 'My Account' section after logging in.
      .email
        = label_tag :email, "Enter your Registered Email Address :"
        = text_field_tag :email, params[:email], :class => 'field'
      %p.button
        = submit_tag "Submit", :class => 'primary-button'
